
import React from 'react';
import classNames from 'classnames';

interface BoxExampleProps {
  
}

const BoxExample: React.FC<BoxExampleProps> = ({  }) => {
  const boxexampleClass = classNames({
    
  });

  return (
    <div className=" items-center flex align-bottom bg-primary text-center text-primary-content w-40 h-40">
        <p className="">This is Example</p>
    </div>
  );
};

export default BoxExample;







