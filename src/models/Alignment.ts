export enum TextAlignment {
    LEFT = 'left',
    RIGHT = 'right',
    CENTER = 'center',
    JUSTIFY = 'justify'
  }

export enum ImageAlignment {
    LEFT = 'left',
    RIGHT = 'right',
    CENTER = 'center',
  }